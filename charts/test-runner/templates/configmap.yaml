apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "test-runner.fullname" . }}
  labels:
    {{- include "test-runner.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{- if $value }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  {{- end }}
