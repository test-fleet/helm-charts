apiVersion: v1
kind: Service
metadata:
  name: {{ include "control-server.fullname" . }}
  labels:
    {{- include "control-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "control-server.selectorLabels" . | nindent 4 }}
